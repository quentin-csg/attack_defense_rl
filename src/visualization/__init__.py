"""Visualization package — Phase 2 Pygame dashboard.

Intentionally empty of imports to avoid loading Pygame at package import time.
Training code (render_mode=None) must never trigger a Pygame import.

Import directly from the sub-modules when needed:
    from src.visualization.renderer import PygameRenderer
    from src.visualization.graph_view import get_node_color, compute_layout
    from src.visualization import theme
"""
